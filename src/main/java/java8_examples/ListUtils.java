package java8_examples;

import java.util.List;

/**
 * @author Evgeny Borisov
 */
public class ListUtils {
    public static <T> void forEachWithDelay(List<T> list, int delay) {
        //todo add third parameter to this method which will be kind of action which should be invoked
        // on each list element with delay between iterations
        // two solutions
        // 1 with your interface
        // 2 with existing interface






//        Thread.sleep(delay);
    }
}
